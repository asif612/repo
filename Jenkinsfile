pipeline {
    agent { label 'New' }
    stages {
        stage('build') {
            steps {
                sh 'pwd'
                sh 'echo welcome'
                sh 'mvn --version'
            }
        }
    }
}
