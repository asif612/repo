pipeline {
    agent { label 'pip' } 
    stages {
        stage { 'checkout' } 
        { 
            steps 
            {
               step 
               {
                echo 'Hello, Git'
                sh "git clone https://github.com/lohitesh/hello-world-war"
            }
        }
            }
      }
}
