pipeline {
    agent { label 'pip' } 
    stages {
        stage('checkout') { 
            steps {
                echo 'Hello, Git'
                sh "git clone https://github.com/lohitesh/hello-world-war"
            }
        }
            }
      }
